# Dự Án Cargo

## Các Tệp

Trong một dự án Cargo truyền thống sẽ có các tệp:

```text
.
├── Cargo.toml
└── src
    └── main.rs
```

Với các tệp:

- `src/main.rs`: là tệp chứa mã gốc
- `Cargo.toml` là tệp chứa thông tin về phần mềm, cấu hình, phiên bản, etc, ...